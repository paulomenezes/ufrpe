<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.paulomenezes.ufrpe">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="b079d87d8d8ab269c4625266e5cf85e3ddec5cab" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            tools:replace="android:theme" />
        <activity
            android:name="br.paulomenezes.ufrpe.start.TipsActivity"
            android:theme="@style/AppThemeNoActionbar" />
        <activity
            android:name="br.paulomenezes.ufrpe.start.SocialActivity"
            android:parentActivityName="br.paulomenezes.ufrpe.start.TipsActivity"
            android:theme="@style/AppThemeNoActionbar" />
        <activity
            android:name="br.paulomenezes.ufrpe.start.ConfirmActivity"
            android:parentActivityName="br.paulomenezes.ufrpe.start.SocialActivity"
            android:theme="@style/AppThemeNoActionbar" />
        <activity
            android:name="br.paulomenezes.ufrpe.start.LoginActivity"
            android:parentActivityName="br.paulomenezes.ufrpe.start.ConfirmActivity"
            android:theme="@style/AppThemeNoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="br.paulomenezes.ufrpe.MainActivity"
            android:label="@string/overview"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="br.paulomenezes.ufrpe.CourseActivity"
            android:parentActivityName="br.paulomenezes.ufrpe.MainActivity" />
        <activity
            android:name="br.paulomenezes.ufrpe.ModuleActivity"
            android:parentActivityName="br.paulomenezes.ufrpe.CourseActivity" />
        <activity
            android:name="br.paulomenezes.ufrpe.ModuleRepliesActivity"
            android:parentActivityName="br.paulomenezes.ufrpe.ModuleActivity" />
        <activity
            android:name="br.paulomenezes.ufrpe.MessageActivity"
            android:parentActivityName="br.paulomenezes.ufrpe.MainActivity" />
        <activity
            android:name="br.paulomenezes.ufrpe.BookDetailActivity"
            android:label="@string/title_activity_book_detail"
            android:parentActivityName="br.paulomenezes.ufrpe.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="br.paulomenezes.ufrpe.MainActivity" />
        </activity>
        <activity
            android:name="br.paulomenezes.ufrpe.RestaurantActivity"
            android:label="@string/restaurant"
            android:parentActivityName="br.paulomenezes.ufrpe.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="br.paulomenezes.ufrpe.MainActivity" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="br.paulomenezes.ufrpe.MapsActivity"
            android:label="@string/map">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="br.paulomenezes.ufrpe.MainActivity" />
        </activity>
    </application>

</manifest>